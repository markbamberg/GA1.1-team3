add.ipynb asks for two numbers, which need to be put in the function "add". It returns the sum of the two numbers.

team multiply used function x = a * b
this multiplies 2 numbers

Team substract created function substract. The substract-function substracts b from a. The function can be called by substract(a, b), it returns the answer.
add.ipynb asks for two numbers, which need to be put in the function "add". It returns the sum of the two numbers.

The sum list function uses the add function to sum up a list of numbers.

explenation for addition subtract: the function returns the distance from zero in absolute value.
